# 项目进度

本文档只记录阶段进展。每完成一个阶段，在这里补一段简短总结。

## 当前阶段

当前处于：阶段 1 准备中。

已有可运行 Demo，下一步进入 MVVM 改造。

## 阶段记录

### 阶段 0：基础 Demo

状态：已完成。

完成内容：

- 使用 Kotlin + XML 搭建 Android 项目。
- 使用 RecyclerView 实现广告信息流。
- 支持精选、电商、本地三个频道。
- 支持搜索、下拉刷新、上滑加载。
- 实现大图、小图、视频三种广告卡片。
- 实现详情页跳转。
- 模拟点赞、收藏、分享、点击、曝光统计。
- 模拟视频播放、暂停、静音状态。

遗留问题：

- 数据和状态仍保存在内存 Repository 中。
- Activity 内承担了较多状态管理和业务调用。
- 还未接入 Room、ViewModel、OkHttp 和真实 AI 服务。

### 阶段 1：MVVM 基础结构

状态：待开始。

目标：

- 整理 `ui / viewmodel / data` 包结构。
- 新增 `FeedViewModel`。
- 新增 `DetailViewModel`。
- 把首页和详情页的业务调用从 Activity 移到 ViewModel。

完成后总结：

- 待补充。

### 阶段 2：Room 数据层

状态：待开始。

目标：

- 新增 `AppDatabase`。
- 新增 `AdEntity`。
- 新增 `AdDao`。
- 将现有 mock 数据写入 Room。

完成后总结：

- 待补充。

### 阶段 3：状态同步

状态：待开始。

目标：

- 列表页和详情页观察同一份 Room 数据。
- 点赞、收藏、分享、点击、曝光全部落库。
- 去掉内存 Repository 中的长期业务状态。

完成后总结：

- 待补充。
