# AI广告推荐信息流

## **实现单列广告信息流**

在内容与广告混排的推荐场景中，用户是否愿意停留，往往取决于 **信息流的呈现质量**（布局、卡片样式、加载体验）以及 **内容理解能力**（摘要、标签、搜索）。本课题要求你们实现一个 **单列广告信息流 App**：客户端实现单列信息流展示、动态化广告卡片与详情页交互，并通过云端大模型为每条广告生成摘要与智能标签，进一步提供对话式搜索体验

### 核心功能

1. 核心交互
   1. 实现单列广告信息流布局，支持流畅滚动与列表位置保持
   2. 广告卡片支持多样式：大图、小图、视频
   3. 顶部 Tab 支持切换频道（如：精选 / 电商 / 本地 ），切换时刷新数据
   4. 点击卡片进入详情页（图文/视频详情），返回保持原位置；
   5. 支持下拉刷新、上拉加载更多
2. 资源复用：
   1. 外流 cell 的复用
   2. 播放器资源的复用
   3. 缓存池的设计
3. 数据/状态同步： 
   1. 数据获取方式（本地 mock 还是走网络请求）
   2. 数据的管理（数据的生命周期，缓存方式）
   3. 数据、状态的传递（跨页面的数据如何传递和同步），比如：详情页和信息流的点评赞状态同步
   4. 视频播放器：暂停/续播、静音/音量控制。比如： 外流视频默认暂停状态，点击按钮进行播放/进入内流自动播放
4. 动画
   1. 页面切换动画
   2. 交互的动效，比如点赞彩蛋等
5. 埋点统计
   1. 支持模拟互动：本地状态管理，点赞、收藏、分享
   2. 且支持展示「曝光/点击」等统计信息（本地模拟 + 可视化展示）

### 可选功能

1. 调用大模型生成每条广告摘要（用于卡片副标题或详情摘要）；生成智能标签（如：品类、风格、受众、场景），并在卡片上展示
   1. 可以使用Qwen 免费版本，部署在本地
   2. 或者实现一个简单模型服务，搭建C/S 框架
2. 支持标签点击过滤（例如只看“运动”“学生党”“性价比”）
3. 提供对话式搜索：用户用自然语言描述想看的广告，返回匹配结果
4. 图片/视频加载与缓存（首屏优先、滚动复用、失败重试）

### 作业要求

1. **技术文档要求**
   1. 必须包含：难点与思考过程、方案对比（至少 2 种）、最终方案与原因、效果评估
   2. 重点建议写清：卡片动态化方案、列表性能策略、AI 输出约束与缓存策略、曝光统计口径
2. **AI 声明（必须）**
   1. 若使用 AI 辅助编程或生成内容，请在 README 中如实注明
   2. 说明：你们对功能的理解、对 AI 结果的验证方式、以及你们做的优化（如结构化输出约束）
3. **实现程度说明（必须）**
   1. 必须实现：核心功能
   2. 可选功能：建议调用大模型接口完成实现，如不能，给出降级方案
4. **工程质量与协作**
   1. 代码可运行、可复现；提供清晰的构建/运行说明
   2. 建议：模块化目录结构、统一代码风格、关键逻辑有注释与边界处理

##  交付物

1. **GitHub 仓库（必需）**
   1. 完整代码 + README（项目介绍、如何运行、模块划分、开发规范、AI 声明）
2. **演示视频（必需）**
   1. 3-8 分钟，放在仓库最外层，展示：信息流浏览 → 刷新加载 → 摘要/标签 → 详情互动 → 对话搜索
3. **技术设计文档（必需）**
   1. 建议放在 `docs/` 目录（Markdown/PDF 均可）
4. **学习总结（可选）**
   1. 说明：棘手问题与解决方案、踩坑复盘、后续可改进点
5. **可选加分材料**
   1. 性能数据对比、曝光统计口径验证、端侧模型 demo、测试用例等