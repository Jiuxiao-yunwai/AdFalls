<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/search_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_app_shell"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    android:paddingStart="32dp"
    android:paddingTop="20dp"
    android:paddingEnd="32dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="62dp"
        android:background="@drawable/bg_header_panel"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="6dp">

        <ImageButton
            android:id="@+id/search_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_dark_button"
            android:contentDescription="返回"
            android:padding="12dp"
            android:scaleType="center"
            android:src="@drawable/ic_nav_back" />

        <EditText
            android:id="@+id/search_input"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:background="@drawable/bg_search_field"
            android:drawableStart="@drawable/ic_search"
            android:drawablePadding="8dp"
            android:drawableTint="@color/app_text_on_dark_muted"
            android:hint="搜索广告、标签或场景"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:paddingStart="14dp"
            android:paddingEnd="16dp"
            android:singleLine="true"
            android:textColor="@color/app_text_on_dark_primary"
            android:textColorHint="@color/app_text_on_dark_muted"
            android:textSize="15sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/search_scope"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="16dp"
        android:textColor="@color/app_text_on_dark_secondary"
        android:textSize="12sp"
        tools:text="当前频道：精选 · 可搜索标题、品牌、摘要和标签" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/search_results"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingBottom="28dp"
            tools:itemCount="3"
            tools:listitem="@layout/item_ad_small" />

        <TextView
            android:id="@+id/search_empty_state"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="输入关键词，搜索标题、品牌、摘要或标签"
            android:textColor="@color/app_text_on_dark_muted"
            android:textSize="14sp" />
    </FrameLayout>
</LinearLayout>
